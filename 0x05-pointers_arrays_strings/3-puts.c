#include "main.h"
/**
 * _puts - This function
 * prints a string to stdout
 *
 * @str: The string variable to print
 *
 */


void _puts(char *str)
{
	int i = 0;

	while (str[i] != '\0')
	{
		putchar(str[i];
		i++;
	}
}
