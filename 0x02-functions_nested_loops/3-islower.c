#include "main.h"

/**
 * _islower - Entry point
 *
 * Function: This function
 * checks if a character
 * is lowercase
 *
 * Return: 1 if argument is lowercase
 * 0 otherwise
 *
 */

int _islower(int c)
{
	if (islower(c))
		return (1);
	else return (0);
}i
